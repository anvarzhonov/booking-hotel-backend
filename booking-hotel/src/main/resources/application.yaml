server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3307/booking_db
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


ru:
  ikbo2019:
    bookinghotel:
      jwtExpiration: 3600

